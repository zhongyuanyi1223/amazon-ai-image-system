# feature

{product_protection}. Answer {buyer_question} with evidence {evidence}. One main message: {message}. Show an honest detail/usage view, no exaggerated performance diagrams. {visible_copy}.

Fill every placeholder from the confirmed Job; never send unresolved placeholders to the image tool.
